# css-expanding-search-bar
A clean, modern, animated search bar built with only HTML and CSS. It starts as a magnifying glass icon and, on focus, smoothly expands into a text field. The icon cleverly transforms from a "search" icon to a "cancel" (X) icon using pure CSS transitions.
